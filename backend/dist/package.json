{
  "name": "bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.ts",
    "clean": "rm -Rf dist && mkdir -p dist",
    "build": "npm run clean && cp package.json dist  && ./node_modules/typescript/bin/tsc -p tsconfig.json "
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-telegram-bot-api": "^0.65.1"
  },
  "devDependencies": {
    "@types/node-telegram-bot-api": "^0.64.6",
    "nodemon": "^3.1.0",
    "ts-node": "^10.9.2"
  }
}
